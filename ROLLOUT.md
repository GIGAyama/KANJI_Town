# GIGA Standard v4 ロールアウト記録

| リポジトリ | 型 | P0 | P1(表示/PWA) | P2 | P3 | ゲート | 備考 |
|---|---|:--:|:--:|:--:|:--:|:--:|---|
| KANJI_Town | B | ✅ | ✅ | ✅ | ✅ | ✅ | 第2群。CSP と外部フォントのローカル化は保留（下記） |

## KANJI_Town（2026-08-03）

診断結果は `AUDIT.md`。着手前の状態と、対応後の状態の両方を記録している。

### 見つかったいちばん大きな問題

**Service Worker が、同一オリジンの他アプリのキャッシュを削除していた。**
`gigayama.github.io` は数十個のアプリでドメインを共有しているため、
このアプリを開くだけで他のアプリがオフラインで起動しなくなっていた。

同じ書き方をしている他のリポジトリがある可能性が高い。**横展開の際は、
まず全リポジトリの `sw.js` で `caches.keys()` の使い方を確認することを勧める。**

```bash
grep -n "caches.keys" sw.js public/sw.js docs/sw.js 2>/dev/null
# 削除の filter に接頭辞（startsWith）が無ければ、他アプリを壊している
```

### あわせて見つかった、着手前からあった不具合

**一度開いたあとネットが切れると画面が真っ白になっていた。**
Service Worker が動き出すのは最初の読み込みより後のため、そのとき読まれた
JS/CSS がキャッシュに残らない。`index.html` を読んで必要なファイルを
先に取り込むようにして解決した。**この構成（Vite + 静的 sw.js）の
リポジトリはすべて同じ状態のはずなので、横展開時に必ず確認する。**

### 品質ゲート

`scripts/check-project.mjs` + `quality.config.json` を新設し、CI に組み込んだ。
33項目を検査する。他リポジトリへはこの2ファイルをコピーし、
`quality.config.json` の `repoName` / `basePath` / 例外リストを実態に合わせる。

わざと壊して検出できることを確認済み（接頭辞チェックの除去 → C4、`id` の削除 → C1）。

### 人間の判断を待っている項目

1. **CSP の投入** — 許可すべきホストは4件に確定済み
   （`cdn.jsdelivr.net` / `unpkg.com` / `fonts.googleapis.com` / `fonts.gstatic.com`）。
   本番相当の通信を通した確認ができる環境で投入したい。
2. **外部フォントのローカル化** — 日本語サブセット数百ファイル（数MB）の同梱が必要。
   容量と豆腐（表示できない文字）の危険をどう見るか。
3. **アイコンの画質** — 688.9KB → 152.0KB に圧縮した。原本は `.assets-original/`。
4. **開発依存の npm audit high 3件**（vite / postcss）— 本番には入らず CI も通る。
5. **`src/data/iso-svg.jsx`（4,860行）の分割方針** — 自動では行わない。
